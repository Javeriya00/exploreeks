# Spring Boot Web Application

This is a simple Spring Boot web application with a UI explaining Terraform, EKS, and Kubernetes components.

## How to Run

1. Clone the repository.
2. Navigate to the project directory.
3. Run the following command:
A simple readme for your project explaining the setup and usage.
Example content:

markdown
Copy
Edit
# Spring Boot Web Application

This is a simple Spring Boot web application with a UI explaining Terraform, EKS, and Kubernetes components.

## How to Run

1. Clone the repository.
2. Navigate to the project directory.
3. Run the following command:
mvn spring-boot:run

markdown
Copy
Edit
4. Open your browser and visit `http://localhost:8080`.
Setting Up the Project in VSCode:
Install the necessary extensions:

Java Extension Pack (for Java support)
Spring Boot Tools (for Spring Boot support)
Thymeleaf for Visual Studio Code (optional, but it helps with syntax highlighting for Thymeleaf templates)
Open the project folder in VSCode:

Once you’ve created the folder and added the files mentioned, open it in VSCode.
Run the application:

Open the terminal in VSCode, navigate to the project folder, and run:

mvn spring-boot:run
This will start the Spring Boot application, and you can open http://localhost:8080 in your browser to view the application.
This project structure should help you get up and running quickly. Let me know if you need further clarification!